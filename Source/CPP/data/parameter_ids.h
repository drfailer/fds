#ifndef PARAMETER_IDS_H
#define PARAMETER_IDS_H

// WARN: this is a temporary file (parameters shouldn't flow into the final
// graph)

enum class ParameterIds {
  None,
  InstabilityCheck,
  TimeStepReduced,
  UpdateTS,
  Start,
  Finished,
};

#endif
